class Api::TodosController < ApplicationController
  # only the CRUD actions
  # new and edit are no longer here
  #new and edit form are in the react side now
  # index, show, create, update, destroy belong here
  # redirect_to no longer belongs here
  #return data, html, xml, json
  #json object notation for passing data 
end
